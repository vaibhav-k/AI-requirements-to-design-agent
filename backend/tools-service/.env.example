# tools-service is deterministic-only: no LLM, no database, no storage
# credentials of any kind belong here - only its own bind address.

TOOLS_SERVICE_HOST=0.0.0.0
TOOLS_SERVICE_PORT=8100
TOOLS_SERVICE_LOG_LEVEL=INFO
