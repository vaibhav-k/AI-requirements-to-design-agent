# Scopes SonarQube/SonarLint to this service only - see the root README's
# "Static analysis / IDE setup" section. Run sonar-scanner from inside
# this directory, or configure SonarLint's "bind to project" per folder
# if using connected mode with a server.
sonar.projectKey=ai-requirements-to-design-agent-tools-service
sonar.projectName=AI Requirements to Design Agent - Tools Service
sonar.sources=src
sonar.tests=tests
sonar.python.version=3.14
sonar.exclusions=**/__pycache__/**,**/.venv/**,**/.mypy_cache/**,**/.pytest_cache/**,**/.ruff_cache/**,src/infrastructure/icons/azure/**
